# Completed Milestones History

Auto-archived from C:\Portfolio\實作進度.md by docs/archive-done.ps1.

| 功能 | 關鍵檔案 | 摘要 |
|------|---------|------|
| Phase 1：專案骨架 + 多語系底座 | `next.config.ts`、`velite.config.ts`、`content/categories.ts`、`src/i18n/*`、`src/app/[locale]/layout.tsx` | Next.js 16（App Router）+ TypeScript + Tailwind v4 scaffold；`next-intl` 雙語 routing（`/zh-TW`、`/en`）+ 語言切換器；`shadcn/ui`（Base UI 版本）+ `lucide-react`；`Velite` 內容層（`WorkEntry` schema，四大分類各建 1 筆假資料驗證欄位差異，例如創作領域無下載點）；`next/font`。連接 GitHub [`tanyangbrocks/Orange-Sheep-Portfolio`](https://github.com/tanyangbrocks/Orange-Sheep-Portfolio.git)。 |
| Phase 2-3：分類篩選 UI + 作品詳情頁 + 動態特效 | `src/components/works-filter.tsx`、`src/app/[locale]/works/[slug]/page.tsx`、`src/components/work-card.tsx`、`src/app/[locale]/template.tsx`、`src/app/[locale]/layout.tsx` | 作品列表頁加上分類/子分類篩選（shadcn Tabs + Badge，client-side 篩選）；新增作品詳情頁（gallery、雙語敘述、外部連結按鈕、下載按鈕依 `downloadUrl` 是否存在顯示，含 `generateStaticParams`/`generateMetadata`）；Framer Motion 卡片 hover + 進場動畫 + 路由層級頁面轉場（`template.tsx`，比 `AnimatePresence` 手動處理簡單）；`ReactLenis` 平滑捲動；Vercel Analytics + Speed Insights 安裝（部署前是安全的 no-op）；`next experimental-analyze` 確認 bundle 體積合理（client chunks 共約 1.1MB 未壓縮，無異常大檔案；`@next/bundle-analyzer` 不支援 Turbopack build，已改用 Next 內建方案）。 |
| Vercel 靜態資源快取設定 | `vercel.json` | 新增 `vercel.json`，對 `/static/*`（Velite 產出、`[name]-[hash:6].[ext]` content-hash 檔名）設定 `Cache-Control: public, max-age=31536000, immutable`，讓 CDN/瀏覽器長期快取而不需 revalidate；因檔名本身含內容雜湊，內容變更會自動產生新檔名，不會有快取到舊內容的風險。 |
| 修正 locale 路由未真正靜態化 | `src/app/[locale]/layout.tsx`、`page.tsx`、`about/page.tsx`、`works/page.tsx`、`works/[slug]/page.tsx` | `npm run build` 檢查發現所有 `[locale]` 路由標示為 `ƒ (Dynamic)` 而非 `○/● (Static)`，即使已有 `generateStaticParams`：原因是 next-intl 的 `requestLocale` 來自 request-time 偵測，若沒有在每個 page/layout 呼叫 `setRequestLocale(locale)` 明確聲明，Next.js 無法在 build time 靜態渲染。修正後改用 `getTranslations`（`next-intl/server`）取代 async component 內不能用的 `useTranslations`/`useLocale` hook 版本；重新 build 後全部路由變成 `● (SSG)`，代表未來部署會直接吃 Vercel CDN 快取而非每次 request 都跑 server render。 |
| 單頁式導覽：scroll-spy 底線指示 + 錨點捲動 | `src/components/site-header.tsx`、`src/app/[locale]/page.tsx`、`src/app/[locale]/works/page.tsx`、`src/app/[locale]/about/page.tsx`、`src/app/[locale]/works/[slug]/page.tsx` | 原本首頁/作品/關於是三個獨立路由，改成合併進首頁的三個區塊（`id="home"/"works"/"about"`），`/works`、`/about` 改為對 `/#works`、`/#about` 的 redirect stub（保留舊連結相容）。`SiteHeader` 改成 client component：用 `window.addEventListener('scroll', ...)` 走訪三個 section 比較 `offsetTop` 決定目前分頁（比 `IntersectionObserver` + 負 bottom margin 更穩健——後者在最後一個 section 比視窗矮時，永遠無法達到「捲動進視窗 55%」的門檻，導致「關於」分頁永遠不會被判定為 active；改用純捲動位置比較後，額外加上「已捲到頁面最底部」的特判，確保短 section 也能正確 active）。分頁底線用 Framer Motion `layoutId` 做滑動動畫；點擊導覽列在首頁上時呼叫 `useLenis()` 的 `lenis.scrollTo()` 平滑捲動並用 `history.replaceState` 更新 hash（不觸發整頁導航），在非首頁（如作品詳情頁）點擊則走一般 `Link` 導航回首頁對應錨點。Header 改 `sticky top-0`，讓使用者捲動時能持續看到分頁指示器變化。 |
| 新增「經歷」單頁區塊 + 首頁分頁改名 | `velite.config.ts`、`src/lib/experiences.ts`、`src/components/experience-timeline.tsx`、`content/experiences/*.yml`、`src/app/[locale]/page.tsx`、`src/components/site-header.tsx`、`messages/*.json` | 新增 Velite `experiences` collection（`role`/`organization`/`period`/`description` 雙語欄位 + 選填 `image` + `startDate` 排序用），比照 `works` 的 schema 風格；`ExperienceTimeline` 元件用 Framer Motion 做進場（`whileInView` 淡入上移，逐筆 stagger delay）+ 圖片 hover 放大兩種動畫，時間軸用左側圓點+豎線呈現，圖片欄位選填（沒有圖片就只顯示文字）。插入首頁「簡介」與「作品」之間（`id="experience"`），`SiteHeader` 的 `SECTIONS` 陣列同步加入，scroll-spy／點擊捲動邏輯不用改（本來就是走陣列泛型邏輯）。導覽列文字「首頁」改名「簡介」（英文對應改 `Intro`，避免和既有 `About` 分頁撞名）。 |
| 資產映射機制（三層查找 + 站台資產登錄 + 內容檢查腳本） | `content/works/resolve-preview-images.ts`、`velite.config.ts`、`src/lib/site-assets.ts`、`scripts/check-placeholder-previews.mjs`、`package.json` | 比照 UE5 `ItemRegistry` 的 `MeshPath` 三層查找（明確覆寫→慣例路徑→通用預設值），在 Velite build time 用 `.transform((value, ctx) => resolvePreviewImages(value, ctx.meta.stem))` 解析 `works.previewImages`：YAML 有填就直接用；沒填就試 `public/works/{slug}.{png,jpg,jpeg,webp,svg}` 慣例路徑（`fs.existsSync` 檢查）；都沒有就 fallback 到 `public/works/placeholder.svg`（此為刻意的行為變更：無圖作品從「不顯示預覽區塊」變成「顯示 placeholder」，已與使用者確認）。三層都用建立測試檔案手動驗證過。新增 `src/lib/site-assets.ts` 集中管理站台層級資產路徑（目前僅 `favicon`/`ogImage` 兩筆，之後新增站台級素材一律加在這裡，不要寫死路徑字串）。新增 `scripts/check-placeholder-previews.mjs`（`npm run check-previews`）比照 UE5 `generate_placeholders.py` 精神，讀 `.velite/works.json` 列出目前吃 Tier 3 placeholder 的作品清單。**除錯過程記錄**：一開始誤以為 Tier 2 慣例路徑沒生效，追查後發現是測試對象選錯——4 筆範例作品中有 3 筆的 YAML 內容本來就已經明確寫入 `previewImages: [/works/placeholder.svg]`（Tier 1 explicit 正確生效，非 bug），只有 `example-project-tool` 真的沒有這個欄位，換成它測試後三層邏輯皆正確運作。詳見 [docs/plan-asset-mapping.md](docs/plan-asset-mapping.md)。 |
| 導覽列分頁改名 + 換 favicon | `messages/*.json`、`src/app/icon.png`、`src/lib/site-assets.ts` | 導覽列文字：簡介→關於我、經歷→背景經歷、作品→代表作品、關於→聯絡我（英文對應調整避免撞名）。favicon 換成使用者提供的 `asset/icon/orange-sheep.png`，用 sharp resize 成 256×256 存成 `src/app/icon.png`（Next.js App Router 檔案慣例，會自動產生 `<link rel="icon">`，不需寫程式碼引用），刪除原本 Next.js 預設的 Vercel 三角形 `favicon.ico`。`site-assets.ts` 的 `favicon` 欄位同步更新註記新路徑（純文件用途，實際不需程式碼引用）。 |
| 簡介區塊改版（左照片右自介）+ 導覽列半透明加 icon | `src/components/intro-hero.tsx`、`src/app/[locale]/page.tsx`、`src/components/site-header.tsx`、`messages/*.json` | 參考使用者提供的另一個作品集截圖，把「簡介」從單純標題+精選作品格線，改成左側照片卡（目前是文字佔位框，等使用者提供真實照片再替換，仿站上其他資產走 `public/` 慣例即可）、右側標題/次標題/敘述段落/三格經歷數字卡/兩顆 CTA 按鈕（「探索作品」連到 `#works`、「與我合作」連到 `#about`）的版型；只模仿排版結構，配色維持站上既有中性色階（`bg-muted`/`border`/`text-muted-foreground`），沒有引入參考圖的藍色系。移除因此變成死碼的 `src/components/hero-text.tsx` 與首頁的「精選作品」格線區塊（`代表作品` 區塊本身已顯示全部作品，不再重複）。首頁文案更新：`heroTitle` 改「哈囉，我是譚揚勳」、`heroSubtitle` 改「你也可以叫我彈羊」（英文版對應譯名為暫定，待使用者確認）。導覽列（`site-header.tsx`）背景改 `bg-background/60`（半透明毛玻璃）並在「Orange Sheep」文字左邊加上 `icon.png` 圖示。 |
| 行動裝置友善性優化（照 docs/plan-mobile-friendly.md 三步驟實作） | `src/components/site-header.tsx`、`src/components/ui/sheet.tsx`、`src/components/work-card.tsx`、`src/components/experience-timeline.tsx`、`src/app/[locale]/works/[slug]/page.tsx` | (1) 導覽列手機版加漢堡選單：裝 shadcn `Sheet`（底層沿用既有的 `@base-ui/react`，無新套件），`md` 以下寬度隱藏橫向 nav、改用 44×44px 觸控區的漢堡按鈕開啟側滑選單，選單內連結/語言切換器都放大觸控區；(2) `HEADER_OFFSET` 從寫死的 96px 常數改成 `useRef` + `ResizeObserver` 動態量測實際 header 高度，scroll-spy 與 `lenis.scrollTo()` 都改吃這個動態值；(3) 三處 `next/image fill`（`work-card.tsx`/`experience-timeline.tsx`/`works/[slug]/page.tsx`）補上 `sizes` 屬性，依各自的 grid 斷點設定，避免手機下載超出實際渲染尺寸的圖（測試時發現目前站上圖都是 `.svg` placeholder，Next.js 對 SVG 不產生 `srcset`/`sizes` 效果，等真實圖片上線後才會實際生效，這是預期行為不是 bug）；順手修：header 的 icon 圖片被 Next.js 標記為 LCP 候選但缺少 `loading=eager`，補上 `priority`。**驗證缺口**：這次除了程式碼審查、DOM/computed style 量測（漢堡按鈕觸控區 44×44px 已確認、`display:none/flex` 切換已確認、header 高度已確認變矮变一致）、`npm run build`/`npm run lint`（皆過）之外，**沒能用自動化工具實際點開手機選單來驗證**——瀏覽器 preview 工具在這次除錯過程中對所有點擊互動（包含跟這次改動完全無關的既有導覽列連結）都停止回應，用完全乾淨的新 dev/production server 重測、加 debug log 追蹤都排除了是程式碼問題（`console.log` sanity check 本身正常執行，只有點擊事件送不到 React handler），研判是這次 session 用到很後面時 preview 工具本身的互動送達出狀況，不是這次改動造成的迴歸。**麻煩使用者實際打開網站，手動點一次手機版漢堡選單按鈕，確認選單會滑出、點選單裡的連結會正確捲動並關閉選單**，若有問題請回報。 |
| 簡介照片輪播 + CTA 按鈕互動優化 | `src/components/photo-carousel.tsx`、`src/components/intro-hero.tsx`、`src/lib/site-assets.ts`、`src/app/[locale]/page.tsx`、`messages/*.json` | 新增 `PhotoCarousel`：最多存 5 張照片（`site-assets.ts` 的 `introPhotos`，目前是空陣列，之後放路徑上去即可），用 Framer Motion `AnimatePresence` + `variants`（enter/center/exit 依方向決定進出位移）做滑動轉場；hover 到照片時左右浮現切換箭頭（`opacity-0 group-hover:opacity-100`，也支援 `focus-visible` 讓鍵盤使用者看得到），循環式切換（最後一張按「下一張」繞回第一張）；沒有照片時退回原本的文字佔位框，不影響現有行為。照片與三顆 CTA 按鈕都加上跟下方作品卡片一致的 hover 浮動+陰影動畫（`whileHover={{y:-4}}` + `hover:shadow-lg transition-shadow`）。新增第三顆按鈕「來點互動」放在「探索作品」「與我合作」右邊（目前是佔位按鈕，尚未接功能，使用者確認之後再補）。修正「探索作品」「與我合作」點擊後是瀏覽器原生錨點瞬間跳轉的問題，改成跟導覽列一樣呼叫 `useLenis().scrollTo()` 平滑捲動（在點擊當下即時量測 `header` 實際高度做偏移，不依賴 `SiteHeader` 內部狀態，兩個元件保持獨立）。**驗證缺口**：這次連續三種完全不同的可點擊元件（Base UI Sheet trigger、一般 `next-intl` Link 的 onClick、單純原生 `<button onClick>`）在 preview 工具的自動化點擊測試裡都验证不到事件送達 React handler，但用乾淨的新 session 重新量測過 DOM 幾何位置是正確的（例如輪播箭頭按鈕座標從詭異的負值，在全新 session 重測後變成合理數字），研判是 preview 工具本身這個 session 用到後段的點擊送達不穩定，不是三處完全不相關的程式碼都剛好同時壞掉。**麻煩使用者實際打開網站手動測試：hover 照片看箭頭會不會浮現、點箭頭圖片會不會滑動切換、點「探索作品」「與我合作」會不會平滑捲動而不是瞬間跳轉**，有問題請回報。 |
| 作品卡片/經歷時間軸進場動畫改為每次滑到都重播 | `src/components/work-card.tsx`、`src/components/experience-timeline.tsx` | 使用者發現「第一次往下滑有淡入上移動畫，回頂端再滑一次就不會再播」，確認是 Framer Motion `whileInView` 的 `viewport={{ once: true }}` 刻意設計（動畫只在元素第一次進入畫面時播放），非 bug。使用者偏好每次都重播，改成 `once: false`。用 wheel 事件模擬捲動驗證：捲出畫面時卡片正確變回 `opacity:0`（初始狀態），捲回同一位置動畫正確重新播放，行為符合預期。 |
| 全站過頂/過底橡皮筋回彈效果 | `src/components/overscroll-bounce.tsx`、`src/app/[locale]/layout.tsx` | 使用者要求：滑到頁面最頂/最底時，繼續往同方向滑動，整個畫面要有被拉住、放開後彈回去的感覺（類似 iOS 原生橡皮筋效果）。專案用 Lenis 自己接管捲動（`ReactLenis root`），會蓋掉瀏覽器原生的橡皮筋回彈，所以要手動實作：新增 `OverscrollBounce` 包住整個 `{children}`（放在 `layout.tsx` 的 `ReactLenis` 內層，全站每個頁面都套用），監聽 `wheel`（觸控板/滑鼠）與 `touchstart/move/end`（手機）事件，只在「已經在頂端還往上滑」或「已經在底端還往下滑」時才攔截並用 Framer Motion 的 `useMotionValue` 給整棵樹加上帶阻尼的 `translateY`（`RESISTANCE=0.4`、最大拉距 `MAX_PULL=80px`），放開手勢或滾輪停止 150ms 後用 spring 動畫彈回 0。**除錯記錄**：判斷「是否在頂端」原本用 `<= 0.5` 嚴格比對，但實測 Lenis 靜止時的 `scroll` 值不會精準等於 0（發現是 8px），導致判斷永遠不成立，改用 `BOUNDARY_TOLERANCE = 12px` 容許誤差後正常運作；另外驗證時一度以為完全沒作用，後來發現是 Framer Motion 的 `MotionValue.set()` 不會同步寫入 DOM（要等下一個animation frame才會反映到 style 屬性上），改用等待兩次 `requestAnimationFrame` 後再檢查，才確認 `transform: translateY(...)` 確實有正確套用、放開後也確實彈回 `transform: none`；頂部/底部、滾輪/觸控四種組合都個別驗證過。 |
| 照片輪播支援手機手指滑動切換 | `src/components/photo-carousel.tsx` | 使用者手機實測回報：手機沒有游標，hover 顯示切換箭頭的機制在觸控裝置上完全用不到。改用 Framer Motion 內建的 `drag="x"` + `onDragEnd` 判斷滑動速度/距離（`swipePower = |offset| × velocity`，門檻沿用 Framer Motion 官方 carousel 範例的 10000）來切換圖片，桌面的 hover 箭頭保留不動，兩種操作方式並存。滑動容器加 `touch-pan-y` 讓瀏覽器原生垂直捲動不受影響（只有水平手勢被 Framer Motion 攔截），圖片本身加 `pointer-events-none` 避免瀏覽器原生的圖片拖曳幽靈效果干擾手勢判斷。**驗證方式**：這次改用真實的 pointer 事件序列（pointerdown → 多次 pointermove 模擬時間間隔 → pointerup，讓 Framer Motion 算出真實速度）模擬滑動，而不是單純點擊——同一個 preview session 裡，這種連續手勢事件確實能正確觸發圖片切換（往左滑正確換下一張、往右滑正確換回上一張），推測是先前 session 點擊測試失靈的問題只影響單次點擊事件，不影響這種連續手勢序列。 |
| 全站按鈕加上點擊彈跳回饋 | `src/app/globals.css`、`src/components/ui/button.tsx`、`src/components/site-header.tsx`、`src/components/intro-hero.tsx`、`src/components/work-card.tsx` | 使用者要求「任何按鈕點擊後要有彈跳感」。用純 CSS（不碰事件處理邏輯，避免重蹈本 session 稍早 Base UI 點擊互動的坑）：`globals.css` 新增 `.tap-bounce` 工具類 + 一條全域 `button:not(:disabled)` 規則，按下時縮到 `scale(0.92)`，放開後用 `cubic-bezier(0.34, 1.56, 0.64, 1)`（會先衝過 1.0 再回彈，是常見的「回彈按鈕」CSS trick）彈回原大小；因為套用範圍是全域 `button` 選擇器，Sheet 關閉鈕、`Tabs` 分類頁籤、輪播箭頭、「來點互動」佔位鈕全部自動吃到，不用逐一修改。原生 `<a>`/`Link` 樣式的「按鈕」（導覽列 4 個連結、品牌連結、手機選單連結、簡介兩顆 CTA、作品卡片整張卡）手動加上 `tap-bounce` class。共用的 `ui/button.tsx`（作品詳情頁下載/外部連結按鈕）拿掉原本的 `active:...:translate-y-px` 小位移，改用 `tap-bounce`；因為它原本的 `transition-all` 會跟自訂的 transition 打架，`.tap-bounce` 的 transition 屬性加了 `!important` 確保彈跳曲線不會被蓋掉。**驗證方式**：純 CSS 效果不牽涉 JS 事件處理，用 `getComputedStyle` 逐一確認導覽列連結、品牌連結、CTA 按鈕、漢堡選單鈕、`ui/button.tsx` 渲染出的 `<a>` 標籤，全部正確吃到 `transitionProperty: transform` + 對應的回彈曲線；`:active` 實際按下狀態的視覺效果無法用自動化工具強制觸發驗證，但因為是單純 CSS 偽類（不像先前的 Base UI 互動元件需要真實事件序列），可靠度高。 |
| 修正過頂回彈定格過久 + 全域彈跳規則吃掉箭頭淡入動畫 | `src/components/overscroll-bounce.tsx`、`src/app/globals.css` | 使用者回報兩個問題：①從頂部往下拉時會「定格很久才彈回去」，沒有自然彈跳感；②電腦版照片輪播 hover 到圖片時，左右切換箭頭不見了。用 preview 工具實測找到根因：①Chrome/Edge 觸控板放開手指後仍會送出長達 1~1.5 秒、逐漸衰減的「慣性捲動」wheel 事件（實測 31 個事件跨 1.56 秒），而原本邏輯每收到一個事件（哪怕 deltaY 只剩 0.5）就會重設 150ms 閒置計時器，導致整個慣性尾巴期間畫面都卡在拉伸狀態，直到真正完全靜止才彈回去；修法是加 `MAX_HOLD_MS=400`，用 `performance.now()` 量測手勢從第一次拉動起經過的實際時間，超過 400ms 就不再等待閒置、直接強制 `release()`，把「卡住」時間上限鎖定在 0.4 秒內；同時把彈回動畫從 `stiffness:300/damping:28`（阻尼比 ~0.81，接近臨界阻尼、幾乎不回彈）改成 Framer Motion 的 `bounce:0.35, duration:0.5` 參數化寫法，讓放開後有明顯但不誇張的回彈手感。②追查 `src/app/globals.css` 的全域 `button:not(:disabled) { transition-property: transform !important }` 規則（上一輪「全站按鈕彈跳回饋」新增），發現它會把箭頭按鈕自己的 `transition-opacity`（`photo-carousel.tsx:79,87`）連同 CTA/卡片的 `transition-shadow` 一起蓋掉，導致 hover 顯示/隱藏從平滑淡入變成生硬瞬間切換；修法是把 `transition-property` 從單一 `transform` 擴充成 `transform, opacity, box-shadow`（涵蓋目前程式碼裡所有按鈕會用到的過渡屬性），維持點擊彈跳感的同時不再犧牲各按鈕自己的 hover 過渡。**驗證方式**：用 preview 工具直接對 `window` 派送模擬慣性尾巴的 wheel 事件序列（前段 6 次強力滑動 + 後段 25 次指數衰減的微小 deltaY），實測確認舊邏輯會被最後一個事件的 150ms 閒置計時器一路拖到 1.5 秒以上；CSS 修正則直接 fetch 編譯後的 CSS chunk 內容比對 `transition-property` 實際輸出，確認從 `transform` 變成 `transform, opacity, box-shadow`。**除錯記錄**：驗證過程中發現 Turbopack dev server 在改動 `globals.css` 後，即使重啟 `npm run dev` 仍持續透過 `304 Not Modified`／舊 `Last-Modified` header 提供修改前的舊 CSS chunk（`.next` 持久化快取沒有正確失效這次的 CSS 內容變更），必須整個刪除 `.next` 目錄重新啟動才會反映最新內容；另外自動化工具用 `dispatchEvent()` 送出的合成滑鼠事件不會觸發瀏覽器原生 `:hover` 偽類，也不能用來驗證 `group-hover` 是否顯示，改用直接讀取編譯後 CSS 內容 + 邏輯推導佐證。 |
